fn operators() { 
    let three_quad = isize::pow(3, 4);
    println!("{}", three_quad);
    let oneplusone = 1 + 1;
    println!("one plus one is {}", oneplusone);
    let twoplusthreetimesfour = 2 + 3 * 4;
    println!("2 + 3 * 4 = {}", twoplusthreetimesfour);
}
fn main() {
    operators();
}